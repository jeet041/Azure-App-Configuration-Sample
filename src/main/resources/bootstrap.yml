spring:
    cloud:
        azure:
            appconfiguration:
                stores:
                -   connection-string: ''
                    #endpoint: ''
                    #managed-identity:
                     #   client-id: ''
                    monitoring:
                        enabled: true
                        refresh-interval: 5s
                        triggers:
                        -   key: sentinel
                            label: dev
                    selects:
                    -   key-filter: /demo-app/
                        label-filter: dev
