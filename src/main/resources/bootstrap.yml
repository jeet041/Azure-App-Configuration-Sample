spring:
  profiles:
    active: dev
  cloud:
    azure:
      appconfiguration:
        stores[0]:
          connection-string: ${APP_CONFIGURATION_CONNECTION_STRING:}
        stores:
          - endpoint: 
        managed-identity:
          client-id: 
